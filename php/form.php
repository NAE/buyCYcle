<?php
	//require_once(SERVER CONNECT PHP FILE);
	
	
	if(isset($_GET)) {
		//Check to see if user is logged in
			//if logged in, proceed
				//Check to see if user already has a bike
				
					//If not, check to see if station has bikes
					//If bikes are available, pick a bike to give
						//Send user message telling them which stall to take from
							
						//If no bikes available, give error message
				//If user already has bike give error
				
			
			//if not logged in, error
			
		
	}
	

?>